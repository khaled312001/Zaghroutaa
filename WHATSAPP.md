# تشغيل تنبيهات الواتساب — المحرّك الداخلي (Baileys)

الواتساب بيشتغل عن طريق **سكربت داخلي مستقل** اسمه `wa-engine.mjs` على نفس السيرفر —
**عملية منفصلة تمامًا عن الموقع**، عشان لو حصل أي ضغط على المحرّك مايأثّرش على الموقع نهائيًا.
بيعيد استخدام مكتبة Baileys الموجودة + الجلسة المحفوظة (من غير متصفح، ومن غير إعادة مسح QR كل مرة).

## الوضع الحالي ✅
- السكربت **متظبّط ومُختبَر وشغّال ومتصل** على السيرفر، وبيبعت التذكيرات من الطابور تلقائيًا.
- اتأكدنا إن الموقع بيفضل شغّال أثناء عمل المحرّك (معزول تمامًا).

## الخطوة الوحيدة الباقية: كرون يفضّل السكربت صاحي ٢٤/٧
من لوحة Hostinger ← **Cron Jobs** ← أضيفي مهمة **كل دقيقة** بالأمر ده (بيشغّل السكربت لو لقاه واقف):

```
pgrep -f "[w]a-engine.mjs" >/dev/null 2>&1 || (cd /home/u405809647/domains/zaghroutaa.com && setsid /opt/alt/alt-nodejs22/root/bin/node wa-engine.mjs >/dev/null 2>&1 </dev/null &)
```

> ملاحظة: لازم تكون `"[w]a-engine.mjs"` (بالأقواس) — دي حيلة تخلّي الفحص ما يطابقش أمر الكرون نفسه بالغلط.

كل دقيقة الكرون بيتأكد إن المحرّك شغّال؛ لو وقع لأي سبب بيرجّعه تاني، والجلسة بترجع لوحدها من غير QR.

## ملاحظات
- **رقم استقبال التنبيهات** بيتحط من **الإعدادات** (والنظام لازم يكون مفعّل).
- لو حالة الداشبورد بقت «ربط مطلوب»، يبقى الجلسة خلصت → هيظهر **QR** في صفحة «التنبيهات الذكية»، امسحيه برقم الإرسال.
- **إيقاف مؤقت**: `pkill -f wa-engine.mjs` (الكرون هيرجّعه؛ للإيقاف النهائي امسحي الكرون الأول).
- **التوكن** متخزّن في الداتابيز (waCronToken)، فمفيش أي تعديل على إعدادات السيرفر.
- **أمان**: الإرسال الآلي من رقم شخصي بيخالف شروط واتساب وفيه احتمال إيقاف؛ استخدمي **رقم مخصص للتنبيهات**.

## معماريّة سريعة
- الموقع (Next.js على الاستضافة المشتركة): الداشبورد + الطابور + توليد التذكيرات.
- `wa-engine.mjs` (عملية منفصلة): بيتصل بواتساب، يسحب الطابور من `/api/wa/outbox`، يبعت، ويبلّغ حالته/الـ QR على `/api/wa/status`.
